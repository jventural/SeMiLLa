% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportar_proyecto.R
\name{exportar_proyecto}
\alias{exportar_proyecto}
\title{Exportar el proyecto completo de una escala (carpeta resultados/)}
\usage{
exportar_proyecto(
  escala,
  dir,
  abreviatura = "TEST",
  efa = NULL,
  ensemble = NULL,
  refinamiento = NULL,
  cv = NULL,
  calidad = NULL,
  fiabilidad = NULL,
  omega = NULL,
  discriminacion = NULL,
  coherencia = NULL,
  criterio = NULL,
  forma_corta = NULL,
  escala_respuesta = NULL,
  escala_sin_refinar = NULL,
  adaptacion = NULL,
  dif = NULL,
  k = NULL,
  nombre_test = NULL,
  autor = "SeMiLLa",
  dpi = 600,
  verbose = TRUE
)
}
\arguments{
\item{escala}{Objeto \code{semilla} final (con items y, idealmente, embeddings/similitud).}

\item{dir}{Carpeta de salida (se crea junto con \code{graficos/} y \code{test_aplicacion/}).}

\item{abreviatura}{Prefijo corto para la escala final y el formulario (p. ej. "PM").}

\item{efa}{Resultado de \code{\link{efa_regularizado}} (tabla 03).}

\item{ensemble}{Resultado de \code{\link{precision_clasificacion}} (tabla 04 + graficos 08-09).}

\item{refinamiento}{Resultado de \code{\link{refinar_escala}} (tabla 05).}

\item{cv}{Resultado de \code{\link{validez_contenido}} (tabla 06 + grafico 03).}

\item{calidad}{Resultado de \code{\link{auditar_redaccion_items}} (tabla 07).}

\item{fiabilidad}{Resultado de \code{\link{fiabilidad_semantica}} (tabla 13 + grafico 04).}

\item{omega}{Resultado de \code{\link{omega_semantico}} (columna omega de la tabla 13).}

\item{discriminacion}{Resultado de \code{\link{discriminacion_semantica}} (tabla 08 + grafico 05).}

\item{coherencia}{Resultado de \code{\link{analizar_coherencia}} (graficos 06-07).}

\item{criterio}{Resultado de \code{\link{validez_criterio_predicha}} (tabla 09).}

\item{forma_corta}{Resultado de \code{\link{forma_corta}} (tabla 10).}

\item{escala_respuesta}{Resultado de \code{\link{sugerir_escala_respuesta}} (test administrable).}

\item{escala_sin_refinar}{Objeto \code{semilla} ANTES de refinar; habilita los
graficos comparativos 10 (EFA sin refinar) y 12 (Sankey sin refinar).}

\item{adaptacion}{Resultado de \code{\link{adaptar_transcultural}} (tabla 11).}

\item{dif}{Resultado de \code{\link{detectar_dif_semantico}} (tabla 12).}

\item{k}{Numero de factores para las figuras EFA-cargas (por defecto, el numero
de dimensiones de la escala).}

\item{nombre_test, autor}{Metadatos del test ensamblado.}

\item{dpi}{Resolucion de las figuras PNG (default 600, calidad de
publicacion).}

\item{verbose}{Mensajes de progreso.}
}
\value{
(Invisible) data.frame con los archivos generados y su estado.
}
\description{
Genera, en un solo llamado, toda la estructura de salida de un proyecto
SeMiLLa: tablas numeradas (01..13) en Excel, la subcarpeta \code{graficos/}
con las figuras del analisis (incluidas las figuras comparativas EFA-cargas y
Sankey \emph{sin refinar} vs \emph{refinado}), la escala final exportada, el
test administrable en \code{test_aplicacion/} y un \code{00_RESUMEN.txt}.
Reproduce la misma coleccion de archivos que el pipeline por lotes, pero a
partir de los objetos ya calculados en la sesion (no recalcula ni vuelve a
llamar al LLM). Es la funcion que usa el boton "Exportar proyecto completo"
de la SeMiLLa App, de modo que la salida desde R y desde la App es identica.
}
