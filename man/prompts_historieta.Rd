% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historieta.R
\name{prompts_historieta}
\alias{prompts_historieta}
\title{Generar prompts para historietas (comic strip) de cada historia}
\usage{
prompts_historieta(
  escala_h,
  api_key,
  n_panels = 6L,
  layout = c("2x3", "3x2", "3x3", "1xN", "Nx1"),
  paleta = c("color", "bn"),
  estilo = NULL,
  protagonista_visual = NULL,
  idioma_prompts = c("en", "es"),
  idioma_bocadillos = c("es", "en"),
  modelo = "gpt-4.1-mini-2025-04-14",
  seed = 2026,
  archivo = NULL,
  verbose = TRUE,
  safe_mode = TRUE,
  tema_sensible = NULL,
  intent_clinico = NULL
)
}
\arguments{
\item{escala_h}{Objeto de clase \code{semilla_historias}.}

\item{api_key}{Clave de OpenAI.}

\item{n_panels}{Numero de paneles por historia (default 6, rango 4-9).}

\item{layout}{Disposicion: \code{"2x3"} (default, 2 filas x 3 columnas),
\code{"3x2"}, \code{"3x3"}, \code{"1xN"} (una fila), \code{"Nx1"} (una columna).}

\item{paleta}{\code{"color"} (default, acuarela calida estilo Gemini) o \code{"bn"}
(line art blanco y negro).}

\item{estilo}{Cadena con el estilo visual. NULL = default segun paleta.}

\item{protagonista_visual}{Ficha visual del personaje. NULL = se construye
automaticamente desde \code{escala_h$personajes} y \code{escala_h$poblacion}.}

\item{idioma_prompts}{\code{"en"} (default, mejor calidad en modelos visuales)
o \code{"es"}.}

\item{idioma_bocadillos}{\code{"es"} (default) o \code{"en"}. Idioma del texto que
aparece DENTRO de los bocadillos en la imagen.}

\item{modelo}{Modelo OpenAI para segmentacion narrativa
(default \code{"gpt-4.1-mini-2025-04-14"}).}

\item{seed}{Semilla para reproducibilidad.}

\item{archivo}{Ruta SIN extension. Si se pasa, escribe Excel con prompts
y instrucciones de uso.}

\item{verbose}{Mostrar progreso.}

\item{safe_mode}{\code{TRUE} (default) activa contingencias anti-bloqueo de
modelos texto-a-imagen (Gemini, Imagen, ChatGPT, etc.) cuando el
contenido aborda temas sensibles. Inyecta un bloque "Intent" educativo,
refuerza manga larga / sin marcas en piel / sin objetos cortantes en la
ficha de protagonista y reglas de consistencia, y sanitiza descripciones
visuales que mencionen cicatrices, cortes, sangre, heridas u objetos
peligrosos. \code{FALSE} desactiva todas estas restricciones (comportamiento
v1).}

\item{tema_sensible}{Vector de temas sensibles activados (\code{"autolesion"},
\code{"suicidio"}, \code{"violencia"}, \code{"abuso_sexual"}, \code{"trastorno_alimentario"},
\code{"consumo"}). Si es \code{NULL} (default), se auto-detecta a partir de los
factores y textos de las historias. Solo tiene efecto si \code{safe_mode = TRUE}.}

\item{intent_clinico}{Lógico. Si es \code{TRUE} inserta un bloque "Intent /
Intencion" al inicio del prompt declarando uso clinico/educativo y la
lista explicita de elementos visuales prohibidos. \code{NULL} (default) =
autodetectar: \code{TRUE} cuando se detecto al menos un tema sensible.}
}
\value{
Data.frame con clase \code{semilla_prompts_historieta} y columnas:
\code{factor}, \code{historia_texto}, \code{n_panels}, \code{prompt}. Cada fila es una
historia con su prompt de historieta completo.
}
\description{
Para cada historia de un objeto \code{semilla_historias}, construye un prompt de
historieta de N paneles listo para pegar en un modelo texto-a-imagen.
Cada panel incluye: titulo en caja, descripcion visual de la escena,
bocadillos de dialogo y de pensamiento. La protagonista mantiene
consistencia visual a lo largo de los paneles. Util para tamizajes
proyectivos con adolescentes con baja comprension lectora o para
versiones ilustradas de instrumentos basados en historias (vignette tests).

Solo cobra LLM: el LLM segmenta la historia en paneles. Las imagenes
finales las genera el USUARIO copiando los prompts en Gemini/ChatGPT/etc.
}
\examples{
\dontrun{
h <- prompts_historieta(
  escala_h        = mi_escala_h,
  api_key         = api_key,
  n_panels        = 6,
  paleta          = "color",
  idioma_prompts  = "en",
  archivo         = "historietas_EPNA"
)
cat(h$prompt[1])  # primer prompt listo para pegar en Gemini
}

}
