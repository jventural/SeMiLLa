% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converger.R
\name{converger_escala}
\alias{converger_escala}
\title{Convergencia psicometrica iterativa}
\usage{
converger_escala(
  x,
  api_key,
  modelo = "gpt-4.1-mini",
  max_iteraciones = 5L,
  paciencia = 2L,
  objetivo = c("APLICAR CON CAUTELA", "LISTA PARA CAMPO", "APLICAR COMO ESCALA GLOBAL"),
  n = 280,
  n_rep = 30,
  umbral_aiken = 0.7,
  umbral_ic = 0.5,
  n_jueces = 10,
  formato_si_falla = TRUE,
  seed = 2026,
  verbose = TRUE
)
}
\arguments{
\item{x}{Objeto \code{semilla} con \code{$items} y \code{$embeddings}.}

\item{api_key}{Clave del proveedor LLM.}

\item{modelo}{Modelo LLM (default "gpt-4.1-mini").}

\item{max_iteraciones}{Tope de vueltas (default 5). No conviene subirlo: el
LLM se agota antes, ver la nota de calibracion arriba.}

\item{paciencia}{Vueltas seguidas sin mejorar tras las que se detiene el
ciclo. Cada vuelta recalcula la compuerta completa, la V de Aiken con
jueces LLM y la discriminacion semantica: del orden de 10 minutos en una
escala de 24 items. Sin este corte el bucle agotaba \code{max_iteraciones}
aunque el puntaje bajara en todas. Use \code{Inf} para el comportamiento
anterior.}

\item{objetivo}{Veredicto con el que se detiene: \code{"APLICAR CON CAUTELA"}
(default), \code{"LISTA PARA CAMPO"} o \code{"APLICAR COMO ESCALA GLOBAL"}.}

\item{n}{Respondientes simulados por replica (default 280).}

\item{n_rep}{Replicas de la simulacion de estructura (default 30).}

\item{umbral_aiken}{V de Aiken minima por item (default 0.70).}

\item{umbral_ic}{Limite inferior del IC de la V minimo (default 0.50).}

\item{n_jueces}{Jueces LLM para la validez de contenido (default 10).}

\item{formato_si_falla}{Si \code{TRUE} (default) y el bucle no alcanza el
objetivo, construye la version de eleccion forzada cuasi-ipsativa.}

\item{seed}{Semilla de la simulacion.}

\item{verbose}{Mostrar progreso.}
}
\value{
Lista con \code{escala} (la mejor version), \code{diagnostico},
\code{historial}, \code{cambios} (item viejo -> nuevo y por que),
\code{forced_choice}, \code{objetivo_alcanzado} y \code{minutos}.
}
\description{
Diagnostica la escala con los tres ejes de la compuerta MAS la validez de
contenido (V de Aiken con jueces LLM) y la discriminacion semantica,
reescribe unicamente los items que fallan, y repite hasta alcanzar
\code{objetivo} o agotar \code{max_iteraciones}.

Cada version se puntua y se devuelve la MEJOR, no la ultima: el bucle puede
empeorar la escala mientras la corrige, y sin esta proteccion devolveria una
version peor que la de partida (verificado en las tres corridas de
calibracion).
}
\seealso{
\code{\link{compuerta_pre_aplicacion}},
\code{\link{optimizar_para_campo}}
}
