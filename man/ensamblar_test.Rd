% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensamblar.R
\name{ensamblar_test}
\alias{ensamblar_test}
\title{Ensamblar el test listo para aplicar}
\usage{
ensamblar_test(
  escala,
  escala_respuesta = NULL,
  forma = c("larga", "corta", "ambas"),
  n_items_corta = 16L,
  forma_corta_obj = NULL,
  por_dimension = TRUE,
  nombre_test = NULL,
  sufijo_larga = "_larga",
  sufijo_corta = "_corta",
  subtitulo = NULL,
  instrucciones = NULL,
  incluir_datos = TRUE,
  datos_solicitados = c("edad", "sexo", "nivel_educativo", "n_hijos", "edad_hijo"),
  orden = c("intercalado", "original", "aleatorio"),
  archivo = NULL,
  formato = c("md"),
  autor = NULL,
  version = "1.0",
  idioma = "es",
  incluir_puntuacion = FALSE,
  tabla_respuesta = TRUE,
  items_inversos = NULL,
  ilustraciones = NULL,
  respuesta_imagen = NULL,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{escala}{Objeto \code{semilla}, \code{semilla_items} o data.frame con
columnas 'item' y 'dimension'.}

\item{escala_respuesta}{Objeto \code{semilla_escala_respuesta} devuelto por
\code{sugerir_escala_respuesta()}. Si es NULL, se usa Likert 1-5 generico.}

\item{forma}{Version a generar: "larga" (default, todos los items),
"corta" (version reducida) o "ambas". Si es "corta" o "ambas" y no se
suministra \code{forma_corta_obj}, se calcula internamente con
\code{forma_corta()}.}

\item{n_items_corta}{Numero de items en la forma corta (solo si forma es
"corta" o "ambas"). Default: 4 por dimension.}

\item{forma_corta_obj}{Objeto ya calculado con \code{forma_corta()}.
Si se suministra, se usa directamente sin recalcular.}

\item{por_dimension}{Logico. Si TRUE (default), la forma corta toma la
misma cantidad de items por dimension.}

\item{nombre_test}{Nombre comercial del test. Si NULL, se deriva del concepto.}

\item{sufijo_larga}{Sufijo para archivos de la forma larga (default "_larga").}

\item{sufijo_corta}{Sufijo para archivos de la forma corta (default "_corta").}

\item{subtitulo}{Subtitulo opcional (p. ej. "Version de investigacion").}

\item{instrucciones}{Texto de instrucciones. Si NULL, se autogeneran segun
el tipo de escala de respuesta.}

\item{incluir_datos}{Logico. Si TRUE (default), incluye seccion de datos
demograficos.}

\item{datos_solicitados}{Vector de campos demograficos. Opciones:
"edad", "sexo", "nivel_educativo", "estado_civil", "n_hijos",
"edad_hijo", "ocupacion", "pais", "codigo".}

\item{orden}{Orden de presentacion de los items:
"intercalado" (default, rota dimensiones), "original" (como en el objeto)
o "aleatorio".}

\item{archivo}{Ruta de salida SIN extension. Si NULL, solo se retorna el
objeto sin escribir en disco.}

\item{formato}{Formatos de exportacion: "md" (default), "docx", "html", "txt".
Se puede pasar un vector con varios.}

\item{autor}{Nombre del autor.}

\item{version}{Version del instrumento (default "1.0").}

\item{idioma}{Idioma del test ("es", "en", "pt").}

\item{incluir_puntuacion}{Logico. Si TRUE, anade anexo con la clave de
puntuacion (no imprimir al respondiente).}

\item{tabla_respuesta}{Logico. Si TRUE (default), imprime los anclajes
en una tabla antes de los items.}

\item{items_inversos}{Vector de numeros o codigos de items que deben
puntuarse de forma inversa.}

\item{ilustraciones}{Lista opcional que asocia ilustraciones a los items
(para tests con apoyo grafico). NULL por defecto.}

\item{respuesta_imagen}{Configuracion opcional de respuesta basada en
imagenes. NULL por defecto.}

\item{seed}{Semilla aleatoria (solo para orden = "aleatorio").}

\item{verbose}{Mostrar progreso.}
}
\value{
Objeto \code{semilla_test} con:
\itemize{
\item \code{nombre_test}, \code{subtitulo}, \code{autor}, \code{version}
\item \code{instrucciones}, \code{datos_demograficos}, \code{anclajes}
\item \code{items_presentacion}: data.frame con los items ordenados
\item \code{texto_md}: cadena de markdown ensamblada
\item \code{archivos}: rutas generadas (si aplica)
}
}
\description{
Construye un cuestionario completo listo para administrar a partir de un
objeto \code{semilla} y (opcionalmente) un objeto de
\code{sugerir_escala_respuesta()}. Incluye nombre del test, autor, version,
seccion de datos demograficos, instrucciones, recordatorio de la escala de
respuesta y los items numerados con su formato de respuesta.

Soporta tres ordenes de presentacion (original, intercalado,
aleatorio) y exporta a Markdown, DOCX, HTML y TXT.
}
\examples{
\dontrun{
test <- ensamblar_test(
  escala           = mi_escala,
  escala_respuesta = esc_llm,
  nombre_test      = "Escala de Estilos de Apego Parental (EEAP)",
  autor            = "Dr. J. Ventura-Leon",
  archivo          = "EEAP_v1",
  formato          = c("md", "docx")
)

# Ver el test en consola
print(test)

# Acceder al texto markdown
cat(test$texto_md)
}

}
